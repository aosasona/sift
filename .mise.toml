[tools]
go = "1.24.3"

[tasks."build:core"]
description = "Build xcframework for iOS using gomobile"
run = "gomobile bind -o ../Sift/Shared.xcframework -target=ios,iossimulator -ldflags=-w ."
dir = "./core"
